<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Instagram</title>
    <link href="/static/css/site.css" type="text/css" rel="stylesheet">
</head>
<body>
    <div class="container">
        {{> header}}

        {{#if message}}
            <h4>{{message}}</h4>
        {{/if}}
        <main>
            {{{body}}}
        </main>
        {{> footer}}
    </div>
</body>
</html>