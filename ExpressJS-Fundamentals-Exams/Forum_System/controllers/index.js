const home = require('./home-controller');
const user = require('./user-controller');
const category = require('./category-controller');
const thread = require('./thread-constoller');
const answer = require('./answer-contoller');

module.exports = {
    home,
    user,
    category,
    thread,
    answer
};