const home = require('./home-controller');
const user = require('./user-controller');
const flight = require('./flight-controller');
const seat = require('./seat-controller');
const cart = require('./cart-controller');

module.exports = {
    home,
    user,
    flight,
    seat,
    cart
};